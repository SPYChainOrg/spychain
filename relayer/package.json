{
  "name": "spychain-autoclaim-relayer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Watches SpyChain for confirmed withdrawals and submits the BSC-side claim (Outbox.executeTransaction) so users don't need a second transaction.",
  "scripts": {
    "check": "node dryrun.mjs",
    "start": "node relayer.mjs"
  },
  "dependencies": {
    "@arbitrum/sdk": "^4.0.2",
    "ethers": "^5.7.2"
  }
}
